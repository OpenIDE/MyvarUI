namespace MyvarUI.SDL
{
    public class WindowsSdl
    {
        /*
        Im not sure but i think this will be virtualy the same as linux impmentation just the dll import change to relect,
        .dll and not .so
        */
    }
}